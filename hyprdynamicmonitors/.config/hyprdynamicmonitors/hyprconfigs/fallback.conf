# Fallback profile: used when no other profile matches (e.g. an unknown
# external monitor with no matching rule, or HDM can't determine lid state).
# Enable whatever is connected at preferred settings.

monitor=,preferred,auto,1
